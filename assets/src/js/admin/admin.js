/**
 * Admin dashboard JS entry point.
 * Currently a placeholder — will grow to handle widget toggles,
 * settings form interactions, and AJAX save calls.
 */
document.addEventListener('DOMContentLoaded', () => {
	console.log('GSAP Motion for Elementor — admin JS loaded.');
});